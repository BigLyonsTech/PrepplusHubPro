MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/?appName=Cluster0
JWT_SECRET=generate-a-random-base64-secret
PAYSTACK_SECRET_KEY=
ANTHROPIC_API_KEY=
# true = OTP codes are echoed back in auth API responses instead of emailed (real email isn't wired up). Dev only.
APP_DEV_MODE=false
